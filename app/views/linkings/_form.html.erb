

<%= simple_form_for [current_user, @linking], remote: true do |f| %>
<%=  f.input :link_id, collection: Link.all, prompt: "Select", label: "Social Media" %>
<%= f.input :url, placeholder: "Enter the complete URL..." %>
<%= f.submit %>
<% end %>
